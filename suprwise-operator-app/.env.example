# API base URL — point this at your backend
# For local dev: leave blank (uses Vite proxy to localhost:8000)
# For production: set to your deployed backend URL
VITE_API_BASE=

# Google Sign-In Client ID (optional — remove the Google button if unused)
VITE_GOOGLE_CLIENT_ID=
